module.exports = {
  container: {
    textAlign: 'center'
  },
  navRow: {
    width: '100%',
    display: 'flex',
    flexDirection: 'column',
    padding: '2em'
  }
}
// this styling seems to work. but it's pretty goofy without using combineStyles so I think I'll leave it here..
// unless I actually bring react-fela in?
